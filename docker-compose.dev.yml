services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: base  # Use base stage only for dev
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development


